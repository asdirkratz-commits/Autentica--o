{
  "name": "@repo/db",
  "version": "0.0.1",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "tsx --env-file ../../.env.local src/migrate.ts",
    "apply-pending": "tsx --env-file ../../.env.local src/apply-pending.ts",
    "migrate:prod": "NODE_ENV=production tsx --env-file ../../.env.local src/migrate.ts",
    "seed": "tsx src/seed.ts",
    "studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@types/bcryptjs": "^2.4.6",
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "^0.30.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "drizzle-kit": "^0.21.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
